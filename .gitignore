node_modules/
.next/
out/

# secrets — never commit
.env
.env.*
!.env.example

# build cache
*.tsbuildinfo

# throwaway debug frames
_debug-shots/

# demo deck — local only, never committed
parallax-demo/
.vercel
.env*

# but DO track the template (must come last to override .env* above)
!.env.example
