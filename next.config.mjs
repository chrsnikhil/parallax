/** @type {import('next').NextConfig} */
const nextConfig = {
  // The clone uses plain <img> tags from the original markup, not
  // next/image, so the optimiser is not in play.
};

export default nextConfig;
